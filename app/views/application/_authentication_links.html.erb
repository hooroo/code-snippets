<% if current_user %>
  <%= link_to(t("sessions.links.sign_out"), logout_path) %>
<% else %>
  <%= link_to(t("users.links.sign_up"), new_user_path) %>
  <%= link_to(t("sessions.links.sign_in"), login_path) %>
<% end %>
